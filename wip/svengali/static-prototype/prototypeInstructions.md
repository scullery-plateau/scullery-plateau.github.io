We wish to produce a single page that will include static prototypes of the main body of the app along with all modals / popups.

The prototype should be built based directly on `plan-v6` and `ui-design`, with particular attention paid to conforming to the rules, guidelines, and preferred behaviors in `generalStyleOverview`.

We should be focusing on how the app will integrate into the general styles of the other Scullery Plateau apps as listed in `generalStyleOverview` so that we can ensure that Svengali fits into that ecosystem.

Write out the prototype in the `prototyping` folder as `static-ui-proto.html`