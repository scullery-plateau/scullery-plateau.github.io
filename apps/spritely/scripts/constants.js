namespace('sp.spritely.Constants',{"sp.common.Utilities":"util"}, ({util}) => {
  return util.buildImmutable({
    bgColorPixelId: 'bgColorPixel',
    clearedPixelId: 'clearedPixel',
    defaultColor: '#999999',
    defaultFilename: 'spritely',
    pixelDim: 10,
  });
});
